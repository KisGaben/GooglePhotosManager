<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="GooglePhotosManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GooglePhotosManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" x:Name="RootPanel">
        <ItemsView ItemsSource="{Binding Users}">
            <ItemsView.ItemTemplate>
                <DataTemplate x:DataType="local:User">
                    <ItemContainer>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Stretch">
                            <TextBlock Text="{Binding Id}" Margin="5"/>
                            <TextBlock Text="{Binding Name}" Margin="5"/>
                            <TextBlock Text="{Binding Email}" Margin="5"/>
                            <TextBlock Text="{Binding Storage}" Margin="5"/>
                        </StackPanel>
                    </ItemContainer>
                </DataTemplate>
            </ItemsView.ItemTemplate>
        </ItemsView>

        <Button Click="newUserButton_Click">Add new user</Button>
    </StackPanel>
</Window>
